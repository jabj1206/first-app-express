extends layout

block content
  .container
    a(href="/register").button.is-link Register
    table.table.is-bordered.is-narrow.is-hoverable
      thead
        tr
          th Name
          th Email
      tbody
        each user in users
          tr
            td= user.name
            td= user.email
    a(href="/logout").button.is-link Logout
